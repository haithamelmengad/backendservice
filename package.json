{
  "name": "assignment",
  "version": "1.0.0",
  "description": "**The service**",
  "main": "server.js",
  "scripts": {
    "test": "curl -X PUT localhost:5000/events -H \"Content-Type: application/json\" -d @events.json",
    "config": "curl -X POST localhost:5000/config -H \"Content-Type: application/json\" -d @config.json",
    "start": "babel-node server.js --presets es2015,stage-2"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.18.2",
    "express": "^4.16.3"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-stage-2": "^6.24.1"
  }
}
